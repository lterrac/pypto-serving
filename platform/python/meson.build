# The extension must be importable as `pypto_serving.platform._native`, so the build tree
# mirrors that package layout under <builddir>/python/. Adding that directory to PYTHONPATH
# is enough to import it (`pypto_serving` resolves as an implicit namespace package).
subdir('pypto_serving')
